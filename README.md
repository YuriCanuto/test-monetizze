## Instalação

```python
Realizar o clone do projeto e entrar na pasta do mesmo.

# criar a pasta vendor
Rodar o comando: "composer update"

# cria o arquivo .env
Rodar o comando: "cp .env.example .env"

# subir o servidor para rodar o projeto
Rodar o comando: "./vendor/bin/sail up"
